<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="NetWorkChat" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary">@color/network_chat_primary</item>
        <item name="android:colorPrimaryDark">@color/network_chat_primary_dark</item>
        <item name="android:colorAccent">@color/network_chat_accent</item>
        <item name="android:textColorPrimary">@color/text_light</item>
        <item name="android:textColorPrimaryInverse">@android:color/black</item>
        <item name="android:statusBarColor">@color/network_chat_primary_dark</item>
        <item name="android:textColor">@color/text_dark</item>
        <!-- 允许使用transitions -->
        <item name="android:windowContentTransitions">true</item>
        <!--是否覆盖执行，其实可以理解成是否同步执行还是顺序执行-->
        <item name="android:windowAllowEnterTransitionOverlap">false</item>
        <item name="android:windowAllowReturnTransitionOverlap">false</item>
        <item name="android:windowBackground">@color/light_grey</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="MyTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/text_dark</item>
        <item name="textAllCaps">true</item>
    </style>

</resources>